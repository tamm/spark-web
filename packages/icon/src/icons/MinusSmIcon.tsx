import * as React from 'react';

import { createIcon } from '../createIcon';

export const MinusSmIcon = createIcon(
  <React.Fragment>
    <path d="M18 12H6" />
  </React.Fragment>,
  'MinusSmIcon'
);
